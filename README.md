# RUFOOF - منصة تصفح الكتب والمبادرات الشبابية

موقع إلكتروني متكامل لعرض الروايات الشبابية، المبادرات، والوظائف المتاحة مع لوحة تحكم كاملة لإدارة المحتوى.

## 🌟 المميزات

- ✅ واجهة عامة حديثة وجذابة
- ✅ لوحة تحكم كاملة لإدارة المحتوى
- ✅ نظام رفع الملفات (صور و PDF)
- ✅ دعم كامل للغة العربية (RTL)
- ✅ تصميم متجاوب مع جميع الأجهزة
- ✅ ألوان حديثة وتأثيرات متحركة
- ✅ خياران للخادم (PHP أو Node.js)

## 📁 هيكل المشروع

```
ro/
├── index.html              # الصفحة الرئيسية
├── about.html              # صفحة من نحن
├── partners.html           # صفحة الشركاء
├── css/
│   └── style.css          # ملف التنسيقات الرئيسي
├── js/
│   └── main.js            # JavaScript للواجهة العامة
├── admin/
│   ├── index.html         # لوحة التحكم
│   ├── css/
│   │   └── admin.css      # تنسيقات لوحة التحكم
│   └── js/
│       └── admin.js       # JavaScript للوحة التحكم
├── api/                   # ملفات PHP (خيار 1)
│   ├── upload.php         # رفع الملفات
│   ├── save-data.php      # حفظ البيانات
│   └── get-data.php       # جلب البيانات
├── data/
│   └── data.json          # قاعدة البيانات (JSON)
├── uploads/               # مجلد الملفات المرفوعة
├── server.js              # خادم Node.js (خيار 2)
└── package.json           # تبعيات Node.js
```

## 🚀 طريقة التشغيل

### الخيار 1: استخدام Node.js (موصى به)

#### 1. تثبيت Node.js
تأكد من تثبيت Node.js على جهازك. يمكنك تحميله من [nodejs.org](https://nodejs.org)

#### 2. تثبيت التبعيات
```bash
cd c:\Users\center_vip\Desktop\ro
npm install
```

#### 3. تشغيل الخادم
```bash
npm start
```

#### 4. فتح الموقع
افتح المتصفح وانتقل إلى:
- **الموقع الرئيسي**: http://localhost:3000
- **لوحة التحكم**: http://localhost:3000/admin

---

### الخيار 2: استخدام PHP

#### 1. تثبيت XAMPP أو WAMP
قم بتحميل وتثبيت أحد البرامج التالية:
- [XAMPP](https://www.apachefriends.org/)
- [WAMP](https://www.wampserver.com/)

#### 2. نقل المشروع
انسخ مجلد `ro` إلى مجلد `htdocs` (في XAMPP) أو `www` (في WAMP)

#### 3. تشغيل الخادم
- شغّل Apache من لوحة تحكم XAMPP/WAMP

#### 4. فتح الموقع
افتح المتصفح وانتقل إلى:
- **الموقع الرئيسي**: http://localhost/ro
- **لوحة التحكم**: http://localhost/ro/admin

---

## 🔐 تسجيل الدخول إلى لوحة التحكم

**بيانات الدخول الافتراضية:**
- **اسم المستخدم**: `admin`
- **كلمة المرور**: `1234`

> ⚠️ **تنبيه أمني**: يُنصح بتغيير بيانات الدخول في ملف `admin/js/admin.js` قبل النشر على الإنترنت.

## 📝 كيفية استخدام لوحة التحكم

### إضافة رواية جديدة

1. سجّل الدخول إلى لوحة التحكم
2. اختر تبويب "الروايات"
3. املأ البيانات التالية:
   - عنوان الرواية
   - وصف الرواية
   - صورة الغلاف (اختياري)
   - ملف PDF (اختياري)
4. اضغط "نشر الرواية"

### إضافة مبادرة جديدة

1. اختر تبويب "المبادرات"
2. املأ البيانات:
   - اسم المبادرة
   - وصف المبادرة
   - صورة المبادرة (اختياري)
3. اضغط "نشر المبادرة"

### إضافة وظيفة جديدة

1. اختر تبويب "الوظائف"
2. املأ البيانات:
   - نوع الوظيفة
   - تاريخ النشر
   - تاريخ الانتهاء
   - الحالة (متاحة/منتهية)
3. اضغط "نشر الوظيفة"

## 🎨 تخصيص التصميم

### تغيير الألوان

افتح ملف `css/style.css` وعدّل المتغيرات في بداية الملف:

```css
:root {
  --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
  --accent-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
  /* ... */
}
```

### تعديل محتوى صفحة "من نحن"

افتح ملف `about.html` وعدّل النصوص داخل عناصر `<div class="content-section">`

### تعديل الشركاء

افتح ملف `partners.html` وعدّل بطاقات الشركاء أو أضف المزيد

## 🔧 تغيير بيانات الدخول

افتح ملف `admin/js/admin.js` وعدّل السطور التالية:

```javascript
const ADMIN_USERNAME = 'admin';  // غيّر اسم المستخدم هنا
const ADMIN_PASSWORD = '1234';   // غيّر كلمة المرور هنا
```

## 📤 النشر على الإنترنت

### النشر باستخدام Node.js

1. ارفع جميع ملفات المشروع إلى الخادم
2. شغّل `npm install` على الخادم
3. شغّل `npm start` أو استخدم PM2 للتشغيل المستمر:
   ```bash
   npm install -g pm2
   pm2 start server.js --name rufoof
   pm2 save
   ```

### النشر باستخدام PHP

1. ارفع جميع الملفات إلى خادم يدعم PHP
2. تأكد من أن مجلد `uploads` و `data` لديهما صلاحيات الكتابة (chmod 777)
3. افتح الموقع من خلال اسم النطاق الخاص بك

## 🛠️ استكشاف الأخطاء

### المشكلة: لا تظهر البيانات في الموقع

**الحل:**
- تأكد من تشغيل الخادم (Node.js أو PHP)
- تحقق من وجود ملف `data/data.json`
- افتح Console في المتصفح للتحقق من الأخطاء

### المشكلة: لا يعمل رفع الملفات

**الحل:**
- **Node.js**: تأكد من تثبيت جميع التبعيات (`npm install`)
- **PHP**: تأكد من صلاحيات مجلد `uploads` (chmod 777)
- تحقق من حجم الملف (الحد الأقصى 10MB)

### المشكلة: لا يتم حفظ البيانات

**الحل:**
- تحقق من صلاحيات مجلد `data` (chmod 777 في PHP)
- افتح Console في المتصفح للتحقق من أخطاء JavaScript
- تأكد من أن الخادم يعمل بشكل صحيح

## 📱 التوافق

- ✅ Chrome
- ✅ Firefox
- ✅ Safari
- ✅ Edge
- ✅ الأجهزة المحمولة (iOS & Android)

## 🔒 الأمان

> ⚠️ **مهم للإنتاج:**
> - غيّر بيانات الدخول الافتراضية
> - استخدم HTTPS في الإنتاج
> - أضف نظام مصادقة أكثر أماناً (JWT, Sessions, etc.)
> - قم بتحديث صلاحيات الملفات بشكل مناسب
> - أضف حماية CSRF للنماذج

## 📞 الدعم

إذا واجهت أي مشاكل:
1. تحقق من قسم "استكشاف الأخطاء" أعلاه
2. افتح Console في المتصفح (F12) للتحقق من الأخطاء
3. تأكد من تشغيل الخادم بشكل صحيح

## 📄 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الحر.

---

**تم التطوير بواسطة Antigravity AI** 🚀

**نتمنى لك تجربة ممتعة مع RUFOOF!** 📚✨
